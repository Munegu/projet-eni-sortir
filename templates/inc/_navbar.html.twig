<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <img alt="logo" src={{ asset('img/play-button.png') }}  >

    <a class="navbar-brand ml-3" >Sortir.com</a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse " id="navbarNavAltMarkup">
        <div class="navbar-nav ml-auto">

            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <a class="nav-item nav-link active " href="{{ path('main_accueil') }}">Accueil<span class="sr-only">(current)</span></a>
                {% if is_granted("ROLE_ADMIN") %}
                    <a class="nav-item nav-link" href="{{ path('main_villes') }}">Villes</a>
                    <a class="nav-item nav-link" href="{{ path('main_sites') }}">Sites</a>
                    <a class="nav-item nav-link" href="{{ path('participant_register') }}">Créer un utilisateur</a>
                {% endif %}
                    <a class="nav-item nav-link" href="{{ path('participant_editprofil') }}">Mon profil</a>
                    <a class="nav-item nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
            {% else %}

            <a class="nav-item nav-link" href="{{ path('app_login') }}">Se connecter</a>

            {% endif %}
        </div>
    </div>
</nav>